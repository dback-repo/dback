package logic

import (
	"log"
)

func Backup(dockerClient string, emulate []string, exclude []string) {
	log.Println(`Backup started`)
}

func Restore(dockerClient string, emulate []string) {
	log.Println(`Make restore...`)
}
